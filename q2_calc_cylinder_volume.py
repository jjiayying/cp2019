#!/usr/bin/env python
# coding: utf-8

# In[ ]:


#finding area
radius = float(input("radius"))
from math import pi
area = radius * radius * pi

#finding volume
length = float(input("length"))
volume = area * length


# In[ ]:


#finding area
radius = float(input("radius"))
from math import pi
area = radius * radius * pi

#finding volume
length = float(input("length"))
volume = area * length


# In[ ]:




